{
  "name": "yarn-workspace-types-repro",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0",
  "description": "repro for a bug with typescript loading types from a workspace root",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.2.4"
  }
}
